
import { routeQuery } from 'demo-day-four/helpers/route-query';
import { module, test } from 'qunit';

module('Unit | Helper | route query');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = routeQuery([42]);
  assert.ok(result);
});

